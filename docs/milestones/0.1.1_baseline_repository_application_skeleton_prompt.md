# Milestone 0.1.1 — Baseline Repository and Application Skeleton

## Required File Names

**Prompt:**

`0.1.1_baseline_repository_application_skeleton_prompt.md`

**Closeout:**

`0.1.1_baseline_repository_application_skeleton_closeout.md`

Store both under:

`docs/milestones/`

---

## Reasoning Level

**Medium**

Milestone 0.1.0 completed the high-reasoning architecture reconnaissance.

This milestone should implement that architecture narrowly rather than reopen it.

---

## Milestone Mode

**Implementation-after-reconnaissance**

Controlling reconnaissance closeout:

`docs/milestones/0.1.0_architecture_runtime_reconnaissance_closeout.md`

Do not repeat broad architecture or runtime reconnaissance.

Implement the smallest safe application scaffold consistent with that closeout.

---

# 1. Goal

Create the minimum working repository/application foundation for the DVD RVE Upscaler browser application.

At the end of this milestone, the repository should contain:

```text
project-local Python dependency/runtime definition
+
minimal FastAPI application
+
simple server-rendered browser page
+
health endpoint
+
approved application package structure
+
basic configuration boundary
+
minimal automated test foundation
+
documented development launch command
```

The application should be startable manually on `henderson-server1` and reachable from the Product Owner's Windows browser over the LAN for a simple scaffold validation.

This milestone must **not** implement:

- NAS movie discovery;
- FFmpeg or ffprobe media analysis;
- DVD progressive/interlace classification;
- media preparation;
- RVE invocation;
- TensorRT processing;
- NVENC processing;
- job execution;
- process supervision;
- telemetry;
- SQLite job persistence;
- validation pipeline;
- NAS publication;
- Jellyfin integration.

The purpose is to establish a clean, tested application foundation upon which 0.1.2 and later milestones can build.

---

# 2. Required Reading

Before coding, read:

```text
docs/context/coding_agent_rules_v1.md
docs/context/project_workflow_v1.md
docs/context/dvd_rve_upscaler_project_plan_v1.md
docs/context/dvd_rve_upscaler_coder_intro_v1.md
docs/milestones/0.1.0_architecture_runtime_reconnaissance_closeout.md
docs/milestones/0.1.1_baseline_repository_application_skeleton_prompt.md
```

Treat the 0.1.0 closeout as the controlling architecture reconnaissance for this milestone.

Do not substitute prior chat memory for repository evidence.

---

# 3. Repository and Runtime

Authoritative repository:

```text
/home/chuck/projects/DVD-RVE-upscaler
```

Authoritative host:

```text
henderson-server1
```

Normal working environment:

```text
VS Code Remote SSH / Linux terminal
```

Initial development runtime:

```text
direct Python process
under user chuck
on henderson-server1
```

Future production runtime:

```text
systemd-managed Python service
LAN-only
```

Systemd is not in scope for this milestone.

---

# 4. Git Preflight

Before substantive work:

```bash
cd /home/chuck/projects/DVD-RVE-upscaler

git branch --show-current
git status --short
git log --oneline --decorate -5
git rev-parse HEAD
git rev-parse '@{upstream}'
```

Expected starting condition:

- current branch is `main` unless repository state says otherwise;
- Milestone 0.1.0 is committed;
- HEAD matches upstream;
- working tree contains only expected current milestone material, if any.

If there are unexpected tracked or untracked files, stop and report before changing code.

Do not:

- switch branches;
- create branches;
- commit;
- push;
- rewrite Git history.

Git writes remain under Product Owner control.

---

# 5. Locked Architecture from 0.1.0

Do not reopen these decisions unless implementation reveals a genuine contradiction.

## 5.1 Web stack

Use:

```text
Python
FastAPI
server-rendered HTML
minimal browser JavaScript
```

Do not introduce:

- React;
- Vue;
- Angular;
- a separate frontend build pipeline;
- a large SPA;
- WebSockets merely for this scaffold;
- an event bus.

## 5.2 Future persistence

Future durable job state will use:

```text
SQLite
```

Do not implement the real job-state schema or orchestration database in this milestone.

## 5.3 Processing integration

Future processing will use controlled subprocesses around:

```text
FFmpeg / ffprobe
RVE backend
```

Do not invoke or wrap those tools in this milestone.

## 5.4 Storage

Future media workflow remains:

```text
read-only NAS original
→ server-local NVMe work area
→ validation
→ controlled NAS publication
```

No media or NAS access is required in 0.1.1.

## 5.5 Network model

The application is:

```text
LAN-only
not public
not routed through Caddy
not externally forwarded
```

Do not change firewall, router, Caddy, Jellyfin, or host networking in this milestone.

---

# 6. Implementation Requirements

## 6.1 Confirm Python Runtime

Perform only the narrow inspection needed to establish the current host Python runtime:

```bash
python3 --version
which python3
```

Record the observed result in the closeout.

Do not:

- replace system Python;
- alter system Python packages globally;
- install another Python distribution unless an actual blocker is found and escalated.

---

## 6.2 Project-Local Python Environment

Create a project-local virtual environment using the existing host Python.

Preferred location:

```text
.venv/
```

Ensure `.venv/` is ignored by Git.

Do not use:

```text
sudo pip
global pip installs
RVE's bundled/private Python environment
```

The DVD RVE Upscaler application's Python environment must remain separate from the installed RVE private Python runtime.

---

## 6.3 Dependency Definition

Create the smallest clear dependency definition needed for this scaffold.

Choose one simple standard mechanism appropriate to this new Python project.

Acceptable examples include:

```text
requirements.txt
```

or another similarly lightweight Python dependency definition if repository conventions or current tooling clearly justify it.

Do not introduce a large packaging/build system merely for the scaffold.

At minimum, include only dependencies actually needed for:

- FastAPI;
- development ASGI serving;
- server-rendered templates;
- automated tests;
- HTTP test client support if required by the selected FastAPI test pattern.

Avoid speculative dependencies for future milestones.

Do not add:

- SQLAlchemy unless actually required now;
- background queue libraries;
- Redis;
- Celery;
- GPU libraries;
- FFmpeg wrappers;
- RVE packages;
- frontend frameworks.

If dependency installation is required into `.venv`, that is authorized for the project-local environment only.

Do not mutate RVE's environment.

---

## 6.4 Repository Structure

Implement the approved minimal structure from 0.1.0.

Use:

```text
app/
  __init__.py
  main.py
  api/
  services/
  state/
  templates/
  static/

tests/

docs/
  context/
  milestones/
```

Add Python package files where they are actually required.

Do not create large placeholder modules merely because later milestones may use them.

Empty future directories may include a minimal README or package marker only if needed to preserve the intended structure in Git.

The application package root is:

```text
app/
```

Do not create a competing `src/` architecture.

---

## 6.5 FastAPI Application

Implement the smallest real FastAPI application in:

```text
app/main.py
```

It should expose an application object suitable for development launch.

Preferred application variable:

```python
app
```

The app should have:

1. a browser landing page;
2. a health endpoint.

Do not implement media or job APIs yet.

---

## 6.6 Browser Landing Page

Create a simple server-rendered page.

The purpose is only to prove:

```text
Windows browser
→ LAN FastAPI app
→ rendered HTML response
```

The page should identify the application clearly, for example:

```text
DVD RVE Upscaler
```

or:

```text
DVD Enhance Assistant
```

A small subtitle/status message may say that the application foundation is operational.

Keep the page intentionally simple.

A useful initial visual structure may include future workflow headings such as:

```text
Select
Analyze
Prepare
Enhance
Validate
Publish
```

but they must be visually inert placeholders only.

Do not create fake functioning buttons or controls for capabilities that do not yet exist.

The page should clearly communicate that this is the application scaffold rather than pretend that processing is operational.

Use simple CSS only.

Do not introduce a frontend toolchain.

---

## 6.7 Health Endpoint

Provide a simple read-only endpoint such as:

```text
GET /health
```

It should return a deterministic success response suitable for:

- automated testing;
- later health monitoring;
- development verification.

Example shape:

```json
{
  "status": "ok"
}
```

Do not include:

- server filesystem details;
- environment variables;
- RVE paths;
- NAS paths;
- system diagnostics;
- secrets.

---

## 6.8 Initial API Organization

Use the approved:

```text
app/api/
```

boundary where sensible.

Do not over-engineer routing.

For this milestone, it is acceptable for the application to have only a very small router structure.

The goal is to establish a clean boundary for future API routes, not to populate future functionality.

---

## 6.9 Services Boundary

Create:

```text
app/services/
```

as the future home for controlled integrations such as:

```text
media probe
analysis
FFmpeg preparation
RVE invocation
validation
telemetry
publication
```

Do not implement those services now.

If package marker files are needed, keep them minimal.

---

## 6.10 State Boundary

Create:

```text
app/state/
```

as the future home for:

```text
SQLite job persistence
state transitions
recovery/reconciliation
```

Do not create the actual database or job schema in this milestone.

Do not create a runtime `.db` file merely to prove SQLite exists.

---

## 6.11 Templates and Static Assets

Use:

```text
app/templates/
app/static/
```

for the browser page.

Keep the initial presentation simple and readable.

No external CDN dependency is required for this milestone.

Prefer locally served HTML/CSS.

Avoid adding JavaScript unless it provides a genuine need for the scaffold.

---

## 6.12 Configuration Boundary

Establish the smallest application configuration boundary needed for future growth.

The scaffold should not hard-code future media paths throughout application code.

It is acceptable to add a small configuration module or settings object for scaffold-safe settings such as:

```text
application name
development host
development port
```

Do not yet encode speculative production secrets or NAS credentials.

Do not include:

- passwords;
- tokens;
- NAS credentials;
- private keys.

If future paths are represented, use clearly named constants/settings and do not activate any media access.

---

## 6.13 Default Development Bind

The application is intended to be reachable from the Product Owner's Windows browser over the trusted home LAN.

For manual development validation, use an explicit LAN-capable bind suitable for the current environment.

A normal development command may be equivalent to:

```bash
uvicorn app.main:app --host 0.0.0.0 --port <approved-port>
```

However:

- choose a port that does not conflict with currently known server services;
- do not modify firewall rules;
- do not modify Caddy;
- do not modify router forwarding;
- do not expose the app publicly.

If the selected port is blocked by existing host firewall rules, do not change the firewall in this milestone.

Record the condition and provide a safe localhost validation instead, then stop for Product Owner decision if LAN validation is materially blocked.

Do not silently alter networking to make the test pass.

---

# 7. Automated Testing

Create a minimal test suite.

At minimum test:

1. application import succeeds;
2. health endpoint returns the expected status;
3. landing page returns HTTP success;
4. landing page contains the application identity.

Use the normal FastAPI testing approach appropriate to the installed dependency versions.

Tests must not:

- touch the NAS;
- invoke FFmpeg;
- invoke ffprobe;
- invoke RVE;
- access the GPU;
- create media files;
- modify system state.

Run the complete test suite for this repository.

Because this is a new scaffold, all tests should be fast.

---

# 8. Manual Validation

After automated tests pass, perform a bounded live development validation.

## 8.1 Server-side validation

Start the FastAPI development process using the project `.venv`.

Confirm locally that:

```text
/
```

and:

```text
/health
```

respond.

Use a safe local tool such as `curl` if useful.

---

## 8.2 Browser validation

If LAN binding works without system mutation, provide the Product Owner with the exact browser URL.

Expected form:

```text
http://192.168.1.173:<port>/
```

Do not assume that exact address if observed host/runtime evidence differs.

The Product Owner should be able to open the page from Windows and confirm the simple scaffold renders.

Do not expose the application beyond the existing LAN.

---

# 9. README / Developer Run Instructions

If the repository does not yet contain a project README or development instructions, add the minimum documentation necessary so a future coder can reproduce the scaffold.

Document:

```text
create/activate .venv
install dependencies
run tests
launch development server
open browser URL
```

Keep instructions concise.

Do not create a large operator guide in this milestone.

If the existing project documentation already provides the correct place for these instructions, follow that repository convention instead of duplicating documentation.

---

# 10. `.gitignore`

Maintain the repository-root `.gitignore`.

It already exists to ignore:

```text
.vscode/
```

Add only project-local artifacts actually introduced by this milestone, such as appropriate Python cache/virtual-environment/runtime test artifacts.

Likely categories may include:

```text
.venv/
__pycache__/
*.pyc
.pytest_cache/
```

Add only what is actually appropriate.

Do not use an oversized generic template containing unrelated languages/tools.

Do not ignore files that should be committed, including dependency definitions or source code.

---

# 11. Explicitly Out of Scope

Do not implement:

- movie discovery;
- filesystem browsing of `/mnt/nas/movies`;
- source-selection UI;
- ffprobe parsing;
- `idet`;
- progressive/interlace classification;
- telecine handling;
- geometry conversion;
- FFmpeg command generation;
- preparation encoding;
- RVE backend invocation;
- TensorRT jobs;
- NVENC jobs;
- job process trees;
- cancellation;
- background workers;
- telemetry;
- CPU/GPU temperature reading;
- SQLite job schema;
- job history;
- media validation;
- Jellyfin naming logic;
- NAS publication;
- NAS write mounts;
- systemd;
- Docker;
- Caddy;
- authentication;
- router/firewall changes.

Do not perform a full movie or short media-processing test.

Do not touch original media.

---

# 12. Safety Boundaries

## 12.1 NAS

No NAS access is required for this milestone.

Do not write to or modify:

```text
/mnt/nas/movies
```

Do not perform publication tests.

## 12.2 Original Media

Do not modify, rename, move, or delete any movie file.

## 12.3 RVE

Do not modify:

```text
/home/chuck/apps/real-video-enhancer
```

Do not install dependencies into RVE's private Python environment.

Do not invoke an RVE processing job.

## 12.4 FFmpeg

Do not perform media transformations.

Version inspection is unnecessary unless a narrow implementation blocker unexpectedly requires it; 0.1.0 already established the runtime.

## 12.5 Shared Server

Do not:

- reboot;
- modify Docker;
- modify Jellyfin;
- modify Caddy;
- modify systemd;
- alter UFW;
- change router settings;
- stop unrelated services;
- kill unrelated processes.

## 12.6 Secrets

Do not expose or commit:

- NAS credentials;
- SSH private keys;
- tokens;
- passwords;
- secret environment values.

---

# 13. Architecture Discipline

This milestone is deliberately small.

Do not introduce infrastructure for hypothetical future requirements.

Prefer:

```text
one FastAPI app
one template
one health route
one small test foundation
one dependency definition
```

over abstraction-heavy scaffolding.

Do not create:

- generic plugin systems;
- service registries;
- dependency injection frameworks beyond normal FastAPI needs;
- repository layers for a database that does not exist;
- queue abstractions;
- generic shell runners;
- generic media pipelines;
- premature job engines.

0.1.2 and later milestones will add functionality when there is an actual requirement.

---

# 14. Validation Requirements

Before closeout, run the appropriate equivalent of:

```bash
python -m pytest
```

using the project-local environment.

Also run:

```bash
git diff --check
```

Review:

```bash
git status --short
git diff --stat
git diff
```

Confirm:

- no original media changed;
- no NAS files changed;
- no RVE files changed;
- no system configuration changed;
- no unrelated repository files changed.

---

# 15. Escalation Protocol

Stop and report:

```text
STATUS: ESCALATION REQUIRED
```

if:

- the host Python version cannot support the selected FastAPI stack;
- project-local dependency installation fails for a material runtime reason;
- the architecture from 0.1.0 cannot be implemented without changing the host/RVE environment;
- the selected FastAPI stack conflicts with existing repository/runtime constraints;
- LAN validation requires a firewall, router, Caddy, or other system mutation;
- an unexpected repository state materially changes scope;
- a dependency choice would require significantly broader architecture than the prompt permits;
- implementing the scaffold appears to require media/RVE/NAS integration;
- the locked architecture is contradicted by current repository/runtime evidence.

Use:

```text
STATUS: ESCALATION REQUIRED

Observed conflict:
Locked architecture or prompt assumption affected:
Files/runtime inspected:
Evidence:
Why proceeding is unsafe or materially broader:
Smallest safe options:
Recommended decision:
Incomplete changes, if any:
```

Do not improvise through a material architecture conflict.

---

# 16. Required Closeout

Create exactly:

`docs/milestones/0.1.1_baseline_repository_application_skeleton_closeout.md`

Do not create an additional implementation report.

---

# 17. Required Closeout Structure

Use:

```markdown
# Milestone 0.1.1 — Baseline Repository and Application Skeleton Closeout

## 1. Scope Completed

Summarize what was implemented.

## 2. Starting Repository State

Record:

- branch;
- starting HEAD;
- upstream relationship;
- initial Git status.

## 3. Python Runtime

Record:

- Python executable;
- Python version;
- project virtual-environment approach.

## 4. Dependency Foundation

Document:

- dependency-definition file;
- dependencies introduced;
- why each dependency is needed.

## 5. Repository Structure Implemented

Show the implemented application/test structure.

## 6. FastAPI Application

Document:

- app entry point;
- landing page;
- health endpoint;
- routing organization.

## 7. Browser / Presentation Foundation

Document:

- template/static structure;
- what the Product Owner sees;
- what is intentionally still non-functional.

## 8. Configuration Foundation

Document the small configuration boundary introduced, if any.

Confirm no secrets were added.

## 9. Test Coverage

Document tests for:

- app import;
- health endpoint;
- landing page;
- application identity.

## 10. Automated Validation

Record exact test command/result.

Record `git diff --check`.

## 11. Manual Runtime Validation

Document:

- development launch command;
- local endpoint validation;
- LAN/browser validation status;
- exact browser URL if successfully validated.

If LAN validation was blocked without an authorized system change, state that clearly.

## 12. Safety Boundaries Preserved

Confirm:

- no NAS writes;
- no original media changes;
- no RVE changes;
- no FFmpeg processing;
- no systemd changes;
- no Docker/Caddy/firewall/router changes.

## 13. Files Changed

List the implementation files created/modified.

## 14. Deviations

State any deviations from the prompt.

If none:

`None.`

## 15. Recommended Next Milestone

Expected direction:

`0.1.2 — Local Media Discovery and Analysis Contract`

Confirm whether reconnaissance findings still support that next step.

## 16. Git Status

Record final:

- branch;
- `git status --short`;
- staged/unstaged state;
- whether unexpected files remain.
```

---

# 18. Definition of Done

Milestone 0.1.1 is complete when:

1. the repository has a project-local Python environment strategy;
2. dependency definitions are committed-ready and minimal;
3. FastAPI is the implemented web framework;
4. `app/main.py` exposes a working application;
5. a simple server-rendered landing page exists;
6. `/health` returns deterministic success;
7. the approved minimal repository structure exists;
8. templates/static boundaries exist;
9. the future service/state boundaries exist without premature functionality;
10. automated tests pass;
11. `git diff --check` passes;
12. the app can be launched manually on `henderson-server1`;
13. local HTTP validation succeeds;
14. LAN/browser validation succeeds if possible without system mutation, or the exact blocker is documented;
15. no media-processing capability is implemented;
16. no NAS write occurs;
17. no RVE environment is changed;
18. no host networking/service configuration is changed;
19. exactly one closeout is produced;
20. the coder stops for Product Owner review.

Do not begin Milestone 0.1.2.

---

# 19. Expected Next Arc

If this milestone closes cleanly, the expected next milestone is:

```text
0.1.2 — Local Media Discovery and Analysis Contract
```

That milestone should begin the first real product capability:

```text
read-only discovery of approved DVD originals
+
ffprobe metadata extraction
+
initial conservative media-analysis contract
```

Do not implement any part of 0.1.2 during this milestone.

Stop after the 0.1.1 closeout and return control to the Product Owner.
