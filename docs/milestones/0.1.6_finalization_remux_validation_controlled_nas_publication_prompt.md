# Milestone 0.1.6 — Finalization, Remux, Validation, and Controlled NAS Publication

## Required File Names

**Prompt:**

`0.1.6_finalization_remux_validation_controlled_nas_publication_prompt.md`

**Closeout:**

`0.1.6_finalization_remux_validation_controlled_nas_publication_closeout.md`

Store both under:

`docs/milestones/`

---

# 1. Milestone Mode

**High-reasoning implementation milestone with mandatory NAS-write safety checkpoint**

Milestone 0.1.5 is accepted, committed, and pushed.

This milestone completes the intended v1 media workflow:

```text
Find DVD
→ Choose Destination
→ Analyze
→ Prepare
→ Enhance
→ Finalize
→ Validate
→ Publish
→ Complete
```

The Product Owner should not need to understand internal local-work paths, remux commands, or NAS mount mechanics.

The application should finish with one correctly named, validated MKV directly in the selected Finished Movie Destination.

For the current Product Owner configuration:

```text
Finished Movie Destination:
DVD Upscaled

Conceptual Windows location:
\\HENDERSON-NAS\Movies\DVD Upscaled
```

The finished file goes directly into that destination root.

Do not create a per-movie subfolder.

---

# 2. Required Reading

Before coding, read:

```text
docs/context/coding_agent_rules_v1.md
docs/context/project_workflow_v1.md
docs/context/dvd_rve_upscaler_project_plan_v1.md
docs/context/dvd_rve_upscaler_coder_intro_v1.md

docs/milestones/0.1.3_ffmpeg_preparation_pipeline_validation_stubs_closeout.md
docs/milestones/0.1.4_rve_backend_integration_job_orchestration_closeout.md
docs/milestones/0.1.5_browser_operator_workflow_library_locations_status_telemetry_closeout.md

docs/milestones/0.1.6_finalization_remux_validation_controlled_nas_publication_prompt.md
```

Treat the accepted 0.1.3–0.1.5 contracts as authoritative.

Do not broadly re-reconnoiter working media-processing architecture.

---

# 3. Git Preflight

Run:

```bash
cd /home/chuck/projects/DVD-RVE-upscaler

git branch --show-current
git status --short
git log --oneline --decorate -5
git rev-parse HEAD
git rev-parse '@{upstream}'
```

Expected:

```text
branch: main
0.1.5 committed and pushed
HEAD == upstream
working tree clean except possibly this active 0.1.6 prompt
```

If materially different:

```text
STATUS: ESCALATION REQUIRED
```

Do not:

```text
create/switch branches
commit
push
rewrite history
```

Git remains under Product Owner control.

---

# 4. Controlling Product Owner End-State

The intended v1 operator flow is:

```text
1. Original DVD rip exists beneath an approved Original DVD Source.

2. Product Owner opens DVD RVE Upscaler.

3. Product Owner finds and selects an eligible original DVD.

4. Product Owner selects:
   DVD Upscaled
   as the Finished Movie Destination.

5. Product Owner analyzes and prepares the DVD.

6. Product Owner runs the approved RVE enhancement.

7. Application finalizes the validated enhanced result.

8. Application validates the final MKV.

9. Application publishes the final MKV directly into:
   DVD Upscaled

10. Product Owner sees a clear completed result.

11. Product Owner may later manually inspect/move the finished movie into:
    Henderson Library/DVD
    for Jellyfin.
```

The application must NOT automatically move the file into the Henderson Library.

The original rip must remain untouched.

---

# 5. Current NAS Organization

Current Product Owner organization:

```text
Movies
|
|-- Henderson Library
|    |-- 4K Ultra HD
|    |-- Blu-ray
|    `-- DVD
|
|-- DVD Original
|
`-- DVD Upscaled
```

Current logical roles:

```text
DVD Original
→ archival/original source

Henderson Library/DVD
→ another approved searchable Original DVD Source during transition
  and final Jellyfin-managed DVD library

DVD Upscaled
→ temporary/staging destination for completed enhanced movies
```

The application stops at `DVD Upscaled`.

No automatic Jellyfin publication or library movement.

---

# 6. Locked Output Naming Contract

Completed application-generated files must use:

```text
<Movie Title> - DVD RVE <Profile Output Suffix>.mkv
```

For the currently approved RVE profile:

```text
Ratatouille - DVD RVE Nomos8k Medium 2x.mkv
```

The controlled suffix for the current profile is:

```text
DVD RVE Nomos8k Medium 2x
```

The Product Owner must NOT manually type or alter this suffix in the normal workflow.

---

# 7. Movie Title Suggestion

The application should suggest the editable Movie Title using this precedence:

```text
1. Recognized original filename convention.

   Ratatouille - DVD Original.mkv
   → Ratatouille

2. Otherwise containing movie folder.

   /Ratatouille/D2_t00.mkv
   → Ratatouille

3. Otherwise source filename stem.

   SomeMovie.mkv
   → SomeMovie
```

Normal UX concept:

```text
Movie title:
[ Ratatouille ]

Generated filename:
Ratatouille - DVD RVE Nomos8k Medium 2x.mkv
```

The Product Owner may edit:

```text
Ratatouille
```

The application owns:

```text
- DVD RVE Nomos8k Medium 2x.mkv
```

---

# 8. Movie Title Safety

Validate the editable title before generating the final filename.

Reject or safely normalize values that could produce:

```text
absolute paths
path traversal
directory separators
empty filenames
"." or ".."
reserved/unsafe filename behavior
unexpected nested directories
```

The title field is a filename component, not a filesystem path.

Do not permit it to escape the selected destination root.

---

# 9. Do Not Use Filename Alone as Safety Identity

Standardized output naming is an important safety signal, but it is not the sole mechanism.

Continue using:

```text
approved source identity
preparation eligibility
durable workflow identity
validated preparation identity
validated RVE job identity
profile identity
output validation
controlled destination identity
```

Already-enhanced files must continue to remain ineligible as ordinary original sources.

---

# 10. Finalization Input Boundary

Finalization may proceed only from an authoritative workflow whose required upstream stages are valid.

At minimum require:

```text
workflow exists
source identity valid
Finished Movie Destination selected
preparation exists
prepared artifact validator PASS
RVE job exists
RVE job state == completed
RVE output validator PASS
approved locked enhancement profile
```

Do not accept:

```text
browser-supplied arbitrary local paths
arbitrary enhanced MKVs
failed jobs
cancelled jobs
interrupted jobs
created/running jobs
unvalidated outputs
```

The server resolves all authoritative paths from durable IDs.

---

# 11. Final Media Assembly / Remux Goal

RVE enhancement has already produced the enhanced video result.

The finalization stage should create a publication-ready MKV while avoiding another lossy video encode.

Preferred model:

```text
validated RVE-enhanced video
+
trusted non-video streams/evidence from prepared input as appropriate
↓
final remux
↓
final local MKV
```

The enhanced video should be stream-copied where technically valid.

Do not re-encode the RVE video merely to perform finalization.

---

# 12. Stream Preservation / Restoration

0.1.4 established that the private RVE backend copies audio and subtitles but does not map chapters or attachments.

0.1.6 must establish the final publication contract.

At minimum investigate and implement the smallest safe final remux that preserves/restores, where present and supported:

```text
enhanced video
audio streams
subtitle streams
chapters
attachments
container/global metadata where appropriate
stream metadata where appropriate
```

Use authoritative source/prepared evidence rather than guessing.

Preferred source hierarchy:

```text
video:
validated RVE enhanced output

audio/subtitle:
use the most trustworthy stream-preserving source that avoids unnecessary encoding

chapters:
prepared/original trusted input

attachments:
prepared/original trusted input

metadata:
preserve useful trusted metadata where safe
```

Do not duplicate streams accidentally.

---

# 13. Mandatory Remux Reconnaissance Checkpoint

Before implementing final remux command construction, inspect the actual stream/container structure of:

```text
validated prepared.mkv
validated completed RVE enhanced.mkv
```

using ffprobe.

Document:

```text
video streams
audio streams
subtitle streams
chapters
attachments
container metadata
stream metadata
```

Determine the exact FFmpeg mapping needed.

Do not assume the RVE output contains exactly the same non-video streams merely because bounded validation saw audio/subtitles.

If the stream structure materially differs from the expected contract:

```text
STATUS: ESCALATION REQUIRED
```

before implementing speculative mapping.

---

# 14. Final Remux Requirements

The final remux must:

```text
use structured subprocess args
shell=False
use server-owned paths only
use local NVMe staging
not overwrite an existing final local artifact silently
retain job/workflow evidence
produce a partial/in-progress artifact first
```

Conceptual local files:

```text
final.partial.mkv
final.mkv
```

Only a validator-PASS final artifact may be promoted to:

```text
final.mkv
```

---

# 15. No Second Video Encode

Preferred video mapping:

```text
-c:v copy
```

or equivalent exact stream-copy behavior.

If an actual technical constraint means the enhanced video cannot safely be stream-copied:

```text
STATUS: ESCALATION REQUIRED
```

Do not silently re-encode.

---

# 16. Audio and Subtitle Policy

Do not introduce unnecessary re-encoding.

Prefer:

```text
audio: copy
subtitles: copy
```

where container-compatible.

If a particular stream cannot be represented safely in MKV through copy:

```text
stop/report
```

rather than silently transcoding or dropping it unless an already-accepted contract explicitly authorizes that behavior.

---

# 17. Chapter Policy

If chapters are present in the trusted preparation/original evidence, the final MKV should preserve them.

If none exist:

```text
chapter count 0 is valid
```

Do not fabricate chapters.

Final validation must compare expected versus final chapter presence/count.

---

# 18. Attachment Policy

If the trusted prepared/original MKV contains attachments:

```text
fonts
cover art
other Matroska attachments
```

preserve them in the final remux where technically supported.

If attachment preservation cannot be implemented safely with the current FFmpeg/MKV pipeline:

```text
STATUS: ESCALATION REQUIRED
```

with exact evidence.

Do not silently claim attachment preservation without validation.

---

# 19. Final Local Validation

Before any NAS publication, validate the local finalized MKV.

Exit code `0` is insufficient.

At minimum validate:

```text
file exists
file non-zero
ffprobe-readable
video exists
video codec expected
width expected
height expected
frame rate within accepted tolerance
duration within accepted tolerance
pixel format expected/acceptable
SAR expected
progressive field state expected
audio stream count/presence
subtitle stream count/presence
chapter count/presence
attachment count/presence where applicable
```

Preserve the accepted duration tolerance:

```text
abs(final_duration - prepared_duration)
<=
max(0.5 seconds, 0.5% * prepared_duration)
```

Do not weaken prior geometry/frame-rate validation.

---

# 20. Finalization State

Use the smallest safe durable state model.

Do not build a generalized orchestration framework.

A finalization/publication record should retain enough evidence to answer:

```text
which workflow
which preparation
which RVE job
which enhancement profile
which movie title
which generated final filename
which destination location
local final artifact path
final validation result
publication state
published path identity
timestamps
failure reason if any
```

Do not duplicate authoritative upstream job/preparation evidence unnecessarily.

---

# 21. Suggested Publication States

Use a bounded explicit state model such as:

```text
created
finalizing
finalized
publishing
published
failed
```

Exact naming may fit repository conventions.

Required semantic rules:

```text
published
```

must mean:

```text
final local validation PASS
+
controlled NAS publication completed
+
published copy validation PASS
```

Do not mark published merely because a copy command returned exit code 0.

---

# 22. Major Safety Checkpoint — NAS Write Authority

This is the first milestone intentionally adding NAS write capability.

Current NAS Movies mount has been read-only.

Do NOT simply make:

```text
/mnt/nas/movies
```

generally writable.

Before any live NAS mutation, inspect current mount/share topology and determine the smallest safe publication boundary.

The target write scope is:

```text
DVD Upscaled
```

only.

Conceptually:

```text
/mnt/nas/movies/DVD Upscaled
```

or another narrowly scoped runtime mount/path that maps only to that destination.

---

# 23. Mandatory NAS Write Architecture Decision

Before changing mounts, credentials, permissions, or publication code, produce a bounded implementation checkpoint describing:

```text
current Movies mount
current read-only behavior
current Synology share/path topology
candidate narrow write mechanism
exact server-visible writable destination
what remains read-only
credential implications
mount/systemd implications
rollback
proof that Henderson Library and DVD Original are not newly writable through the app path
```

Preferred outcome:

```text
existing broad Movies source remains read-only
+
separate narrowly scoped writable publication path exists for DVD Upscaled
```

Do not weaken the entire Movies mount merely because publication needs one destination.

---

# 24. Stop-and-Report Rule for NAS Writes

If the actual Synology/share structure does not permit a clean narrow write boundary without:

```text
making the entire Movies hierarchy writable
changing broad NAS permissions
creating a risky credential scope
changing unrelated mounts
```

then stop with:

```text
STATUS: ESCALATION REQUIRED
```

Do not proceed with broad write access.

Provide the smallest safe options for Product Owner decision.

---

# 25. No Unauthorized NAS Mutation During Reconnaissance

Before Product Owner approval of the exact write architecture:

Do not:

```text
remount
change /etc/fstab
change systemd mount units
create Synology shares
change Synology ACLs
change credentials
write test files
mkdir on NAS
copy media
rename media
delete media
```

Read-only inspection is allowed.

If a system-level change is required, stop and present it before performing the mutation unless the prompt's existing authorized implementation boundary unambiguously permits it.

---

# 26. Publication Destination Identity

Publication must use the selected durable:

```text
FINISHED_DESTINATION location_id
```

For the current intended destination:

```text
DVD Upscaled
```

The application must derive the actual writable runtime path server-side.

The browser must not supply an absolute filesystem destination.

---

# 27. Root-Only Publication

The normal publication model is:

```text
DVD Upscaled/
    Ratatouille - DVD RVE Nomos8k Medium 2x.mkv
```

NOT:

```text
DVD Upscaled/
    Ratatouille/
        Ratatouille - DVD RVE Nomos8k Medium 2x.mkv
```

The legacy/internal:

```text
destination_relative_folder
```

field from 0.1.5 must not reintroduce nested movie-folder behavior.

---

# 28. Publication Collision Policy

Never silently overwrite an existing destination file.

If:

```text
DVD Upscaled/Ratatouille - DVD RVE Nomos8k Medium 2x.mkv
```

already exists:

```text
STOP publication
```

and report a clear collision.

Do not:

```text
overwrite
auto-delete
auto-version
append (1)
replace existing file
```

unless explicitly authorized in a future design decision.

The existing destination file is evidence and must be preserved.

---

# 29. Publication Mechanics

Use controlled staged publication.

Preferred conceptual sequence:

```text
validated local final.mkv
↓
copy to controlled NAS partial name
↓
validate copied NAS artifact
↓
atomic/safe rename within same destination filesystem
↓
final generated filename
```

Example conceptual partial:

```text
.Ratatouille - DVD RVE Nomos8k Medium 2x.partial.mkv
```

or another application-owned safe name.

Do not expose partial artifacts as completed media.

If atomic rename semantics cannot be relied upon on the mounted NAS filesystem:

```text
document exact behavior
```

and use the safest available same-filesystem promotion method.

---

# 30. Partial Publication Failure

If NAS copy fails:

```text
do not mark published
retain local final artifact
retain durable failure evidence
do not delete source
do not delete RVE result
do not delete preparation
```

A partial NAS artifact may be retained for evidence or safely removed only if the application can prove it owns that exact partial artifact.

Never remove an unrelated destination file.

---

# 31. Published Copy Validation

After copying to the NAS but before final success, verify the published candidate.

At minimum:

```text
exists
non-zero
expected size/evidence
ffprobe readable
expected video geometry
expected frame rate
duration tolerance
expected stream counts
chapter count
attachment count where applicable
```

Prefer using the same final validator against both:

```text
local final
NAS published candidate
```

where appropriate.

Only after PASS should publication be declared successful.

---

# 32. Post-Publication Source Safety

Successful publication must NOT:

```text
delete original DVD rip
move original DVD rip
rename original DVD rip
delete prepared.mkv
delete RVE enhanced output automatically
move anything into Henderson Library/DVD
trigger Jellyfin
```

Work-artifact cleanup is separate future scope unless already explicitly authorized.

For v1 safety, retain local evidence.

---

# 33. Completion UX

After final publication PASS, show a clear completion state such as:

```text
Movie Complete

Ratatouille

Profile:
DVD RVE Nomos8k Medium 2x

Final file:
Ratatouille - DVD RVE Nomos8k Medium 2x.mkv

Destination:
DVD Upscaled

Final validation:
PASS

Publication:
Complete
```

Do not make the Product Owner inspect Linux paths.

Advanced Details may show technical evidence.

---

# 34. Pre-Publication UX

Before publication, show the generated filename and destination for Product Owner review.

Conceptually:

```text
Movie title:
[ Ratatouille ]

Generated filename:
Ratatouille - DVD RVE Nomos8k Medium 2x.mkv

Destination:
DVD Upscaled

[ Finalize and Publish ]
```

The exact button wording may be split if safer:

```text
[ Finalize ]
[ Publish ]
```

Either is acceptable if the state transitions remain clear.

Do not publish simply because RVE enhancement completed.

Publication requires an explicit Product Owner action.

---

# 35. Explicit Publication Authorization

The first real NAS publication must require deliberate Product Owner initiation from the browser.

Do not automatically publish at RVE completion.

The Product Owner should be able to inspect:

```text
movie title
generated filename
destination
finalization readiness
```

before initiating publication.

---

# 36. Existing Completed 0.1.4 Artifact

The existing completed full-source job may be used for bounded finalization validation if still present and authoritative:

```text
preparation_id:
96e28311e117419f932b844237154195

RVE job:
12770be3979245229e7fecd7a61ba061

state:
completed

validation:
PASS
```

Do not rerun the entire RVE enhancement merely to test 0.1.6.

If the existing artifact is suitable, reuse it.

---

# 37. Live Publication Candidate

For first Product Owner end-to-end publication, prefer an existing fully enhanced movie rather than running another full enhancement.

`The Usual Suspects` may be used if:

```text
its local enhanced artifact still exists
its preparation remains validator-PASS
its RVE job remains completed/PASS
```

However, the Product Owner's current naming/location state must be respected.

Do not overwrite an existing NAS copy if one already exists.

If a collision exists, use a different safe validation approach or stop for Product Owner direction.

---

# 38. Do Not Modify Existing Henderson Library Media

During 0.1.6 validation, do not modify or overwrite files already located under:

```text
Henderson Library
```

The intended publication area is:

```text
DVD Upscaled
```

only.

---

# 39. API Contract

Add only the APIs required for:

```text
movie title suggestion/update
finalization plan/status
final local validation
explicit publication
publication status
```

A conceptual shape might include:

```text
GET  /api/workflows/{workflow_id}/finalization
POST /api/workflows/{workflow_id}/finalization
POST /api/finalizations/{id}/finalize
POST /api/finalizations/{id}/publish
GET  /api/finalizations/{id}
```

Exact routing may follow existing repository conventions.

Do not expose arbitrary path publication APIs.

---

# 40. Browser Workflow Integration

Extend the accepted 0.1.5 operator workflow rather than creating a second workflow.

Conceptual final stages:

```text
1. Find DVD
2. Destination
3. Analyze & Prepare
4. Enhance
5. Finalize & Publish
6. Complete
```

If retaining five visible stages is cleaner, finalization/publication may live within Completion.

The user should experience one continuous workflow.

---

# 41. Existing Workflow Compatibility

Do not invalidate existing:

```text
library location records
operator workflows
preparation plans
RVE job history
0.1.4 completed job evidence
0.1.5 Product Owner configuration
```

Current Product Owner configuration should remain:

```text
Original DVD Sources:

DVD Intake
disabled

DVD Originals
enabled

Library DVDs
enabled

Finished Movie Destination:

DVD Upscaled
enabled/configured
```

---

# 42. Telemetry

Keep the accepted 0.1.5 telemetry behavior.

No new telemetry architecture is required.

During finalization/publication, telemetry may remain visible but is informational only.

Do not add automatic thermal control.

---

# 43. Publication Progress

Do not invent progress.

If actual byte-copy progress can be obtained safely, display it.

Otherwise show honest state:

```text
Finalizing...
Publishing...
Validating published file...
```

with elapsed time.

Do not estimate fake percentages.

---

# 44. Error UX

Present bounded, human-readable failure messages.

Examples:

```text
Finalization failed.
The local final MKV did not pass validation.

Publication blocked.
A file with this name already exists in DVD Upscaled.

Publication failed.
The local validated copy is still safe on the server.

Publication unavailable.
The DVD Upscaled destination is not currently writable.
```

Raw FFmpeg/copy/system detail belongs under Advanced Details/logs.

---

# 45. Logging / Evidence

Retain local evidence for:

```text
finalization plan
movie title
generated output filename
exact structured remux command
FFmpeg output
local final validation
publication start
destination identity
copy/promotion result
published validation
failure reason
timestamps
```

Do not expose unrestricted raw server filesystem browsing in the browser.

---

# 46. Testing — Naming

Add focused tests for:

```text
recognized " - DVD Original" title extraction
folder-name fallback
filename-stem fallback
Product Owner title edit
profile-owned suffix
generated filename
unsafe title rejection
path separator rejection
empty title rejection
output name cannot escape destination
```

---

# 47. Testing — Finalization Input

Test:

```text
valid completed workflow accepted
missing preparation rejected
failed preparation rejected
missing RVE job rejected
running job rejected
failed job rejected
cancelled job rejected
interrupted job rejected
completed but validator-failed output rejected
browser arbitrary path rejected
```

---

# 48. Testing — Remux

Test with controlled fixtures:

```text
enhanced video selected
video stream copied
audio preserved
subtitles preserved
chapters preserved
attachments preserved where present
metadata behavior documented/tested
no duplicate streams
no second video encode
partial final artifact promoted only after PASS
```

Do not require a full movie RVE run in automated tests.

---

# 49. Testing — Final Validator

Cover:

```text
readable final MKV
geometry
frame rate
duration
SAR
field/progressive state
audio counts
subtitle counts
chapter counts
attachment counts where applicable
missing output
zero-byte output
corrupt output
wrong geometry
wrong fps
duration failure
```

---

# 50. Testing — Destination Safety

Test:

```text
only FINISHED_DESTINATION accepted
disabled destination rejected
unapproved path rejected
absolute browser path rejected
path traversal rejected
legacy destination subfolder cannot redirect publication
root-only destination enforced
```

---

# 51. Testing — Collision

Test:

```text
destination final name absent → publication may proceed
destination final name exists → publication blocked
existing destination remains unchanged
no automatic rename/version
```

---

# 52. Testing — Publication Staging

Using local filesystem fixtures that simulate NAS behavior, test:

```text
partial publication file created
final filename not exposed prematurely
copy failure leaves state failed/not published
only app-owned partial may be cleaned safely
successful copy validated
promotion occurs only after candidate validation
published state only after final validation
```

Do not require real NAS mutation in automated tests.

---

# 53. Testing — Restart / Durable State

Test:

```text
created finalization survives restart
finalized local artifact survives state-store reopen
failed publication remains failed
published record remains published
no automatic republish
no automatic retry
no automatic overwrite
```

---

# 54. Automated Validation

Before closeout run:

```bash
cd /home/chuck/projects/DVD-RVE-upscaler
source .venv/bin/activate

python -m pytest
python -m compileall -q app
git diff --check
git status --short
git diff --stat
```

Review the diff specifically for:

```text
broad NAS write authority
arbitrary browser paths
silent overwrite
automatic publication
video re-encode
dropped chapters
dropped attachments
stream duplication
source deletion
Henderson Library mutation
Jellyfin integration
cleanup scope creep
0.1.7 work
```

---

# 55. Live Validation — Phase A: Read-Only / Local

Before enabling any NAS write:

1. verify authoritative completed preparation/RVE artifacts;
2. inspect actual stream structures;
3. generate movie-title suggestion;
4. generate standardized filename;
5. perform final remux locally;
6. validate the local final MKV;
7. show the planned destination;
8. prove no NAS write has yet occurred.

Do not progress to live publication until Phase A passes.

---

# 56. Live Validation — Phase B: NAS Write Boundary

Before copying a movie:

Verify and document:

```text
exact writable runtime path
exact destination location mapping
Movies broad source remains read-only where intended
DVD Original not writable through publication path
Henderson Library not writable through publication path
DVD Upscaled is the only approved write target
```

Perform only the minimum harmless bounded write check necessary if Product Owner authorization is required and received.

Do not use another Movies folder as a test target.

---

# 57. Product Owner Checkpoint Before First Real Publication

Before first real movie publication, stop and present:

```text
Movie title
Generated filename
Local final validation result
Selected destination
Exact server publication target
NAS write-boundary evidence
Collision check result
```

Then wait for Product Owner approval to perform the real publication.

Do not silently proceed merely because tests pass.

---

# 58. Product Owner Publication Validation

For the approved first real publication, verify:

```text
explicit Product Owner publication action
publication enters publishing state
final file appears directly in DVD Upscaled
no movie subfolder created
filename matches generated contract
no existing file overwritten
published artifact validator PASS
browser reports Publication Complete
original source remains untouched
Henderson Library remains untouched
```

---

# 59. Product Owner Filesystem Validation

Product Owner should be able to see from Windows/NAS that the final structure is:

```text
\\HENDERSON-NAS\Movies\DVD Upscaled\
    <Movie Title> - DVD RVE Nomos8k Medium 2x.mkv
```

and NOT:

```text
\\HENDERSON-NAS\Movies\DVD Upscaled\<Movie Title>\
```

No extra temporary file should remain after successful publication unless intentionally retained/documented.

---

# 60. Product Owner Media Validation

After publication, the Product Owner should perform a bounded playback sanity check of the published MKV.

At minimum:

```text
opens/plays
video visible
audio present
subtitles available if expected
chapters available if expected
duration plausible
```

This is a sanity check, not an exhaustive movie watch.

---

# 61. Explicitly Out of Scope

Do not implement:

```text
automatic movement into Henderson Library/DVD
Jellyfin scan/refresh
deletion of originals
automatic relocation of originals
automatic local-work cleanup
Blu-ray workflow
4K UHD workflow
multiple new RVE profiles
arbitrary model selection
job queue
parallel GPU jobs
automatic retries
automatic republish
automatic overwrite/versioning
public exposure
authentication
Windows desktop native application
general NAS file manager
general writable Movies mount
```

---

# 62. Windows Shortcut / Service Scope

Do not expand 0.1.6 into service/launcher setup.

The intended Product Owner launch remains:

```text
Windows shortcut
→ browser application
```

Systemd/autostart and Windows shortcut finalization should be handled in a small post-0.1.6 finishing milestone unless a prerequisite is discovered.

---

# 63. Escalation Protocol

Stop and return:

```text
STATUS: ESCALATION REQUIRED
```

if:

- narrow `DVD Upscaled` write authority cannot be established without broad Movies write access;
- Synology topology requires a materially different share/mount strategy;
- RVE output and prepared input cannot be remuxed without another video encode;
- chapters/attachments cannot be preserved as expected;
- final stream mapping is ambiguous;
- final validation would require weakening accepted 0.1.3/0.1.4 checks;
- destination collision cannot be handled without overwrite risk;
- NAS filesystem behavior prevents safe staged publication;
- current durable workflow cannot safely identify the authoritative completed RVE result;
- implementation would require modifying Henderson Library media;
- implementation would require deleting or moving original media.

Use:

```text
STATUS: ESCALATION REQUIRED

Observed conflict:
Locked assumption affected:
Evidence:
Files/state inspected:
Media implication:
NAS safety implication:
Why proceeding broadens risk/scope:
Smallest safe options:
Recommended decision:
Incomplete changes, if any:
```

Do not improvise through a safety boundary.

---

# 64. Required Closeout

Create exactly:

```text
docs/milestones/0.1.6_finalization_remux_validation_controlled_nas_publication_closeout.md
```

Do not create additional implementation reports.

---

# 65. Required Closeout Structure

Use:

```markdown
# Milestone 0.1.6 — Finalization, Remux, Validation, and Controlled NAS Publication Closeout

## 1. Scope Completed

## 2. Starting Repository State

## 3. Product End-to-End Workflow

## 4. Finalization Input Contract

## 5. Movie Title Suggestion

## 6. Generated Output Naming Contract

## 7. Prepared vs RVE Stream Reconnaissance

## 8. Final Remux Contract

Record exact FFmpeg mapping and codecs/copy behavior.

## 9. Chapter Preservation

## 10. Attachment Preservation

## 11. Metadata Preservation

## 12. Final Local Artifact Boundary

## 13. Final Local Validator

## 14. Finalization Durable State

## 15. NAS Write Architecture Checkpoint

Document exact before/after mount/write boundary.

## 16. Finished Destination Runtime Mapping

## 17. Collision Protection

## 18. Publication Staging / Promotion

## 19. Published Artifact Validation

## 20. Browser Finalization / Publication UX

## 21. API Contract

## 22. Automated Test Coverage

## 23. Automated Validation

## 24. Read-Only / Local Live Validation

## 25. Product Owner NAS Write-Boundary Approval

## 26. Product Owner Publication Validation

## 27. Product Owner Filesystem Validation

## 28. Product Owner Playback Sanity Validation

## 29. Safety Boundaries Preserved

## 30. Files Changed

## 31. System / Mount Changes

If none:

`None.`

Otherwise document exact authorized mutation and rollback.

## 32. Deviations

If none:

`None.`

## 33. Risks / Open Questions

Only genuine unresolved items.

## 34. Recommended Next Milestone

Expected small finishing milestone:

`0.1.7 — Service Activation, Windows Launcher, and v1 Finishing`

## 35. Acceptance Matrix

## 36. Final Status

## 37. Git Status
```

---

# 66. Mandatory Acceptance Matrix

Every Definition of Done item below must be marked:

```text
PASS — <concrete evidence>
FAIL — <reason>
NOT TESTED — <reason>
NOT APPLICABLE — <reason>
```

No required item may remain:

```text
FAIL
```

or:

```text
NOT TESTED
```

when declaring Milestone 0.1.6 complete.

Product Owner validation is required for the first real publication.

---

# 67. Definition of Done

Milestone 0.1.6 is complete when:

1. finalization accepts only authoritative completed/PASS workflows;
2. arbitrary local finalization paths are rejected;
3. Movie Title is suggested from recognized original filename when available;
4. folder fallback works;
5. filename-stem fallback works;
6. Product Owner can edit/confirm Movie Title;
7. unsafe Movie Title values are rejected;
8. approved profile owns the output suffix;
9. generated filename follows `<Movie Title> - DVD RVE <Profile Output Suffix>.mkv`;
10. current profile generates `DVD RVE Nomos8k Medium 2x`;
11. actual prepared and RVE stream structures are documented;
12. enhanced video is used as final video;
13. finalization does not perform another video encode;
14. audio is preserved appropriately;
15. subtitles are preserved appropriately;
16. chapters are preserved when present;
17. attachments are preserved when present and supported;
18. metadata behavior is explicitly documented;
19. no unintended duplicate streams are created;
20. local finalization uses a partial artifact;
21. partial local artifact is promoted only after validator PASS;
22. local final MKV is ffprobe-readable;
23. local final geometry is correct;
24. local final frame rate is correct;
25. local final duration passes accepted tolerance;
26. local final audio evidence passes;
27. local final subtitle evidence passes;
28. local final chapter evidence passes;
29. local final attachment evidence passes where applicable;
30. durable finalization/publication state exists;
31. existing prep/RVE authoritative state is not duplicated incorrectly;
32. current 0.1.5 workflows remain readable;
33. current 0.1.4 RVE history remains readable;
34. selected Finished Movie Destination uses durable location identity;
35. browser cannot specify arbitrary NAS path;
36. publication destination is root-only;
37. legacy destination subfolder data cannot create nested publication;
38. broad Movies mount is not casually made writable;
39. exact NAS write boundary is documented before mutation;
40. Product Owner approves the live write architecture before first publication;
41. only approved Finished Movie Destination is writable through the publication path;
42. DVD Original remains protected from application publication writes;
43. Henderson Library remains protected from application publication writes;
44. source files are never deleted/moved;
45. final destination filename collision is checked;
46. existing destination file is never silently overwritten;
47. publication uses a controlled partial/staging name;
48. failed publication is not marked published;
49. failed publication retains safe local final artifact;
50. only application-owned partial publication evidence may be cleaned;
51. published candidate is validated on NAS;
52. final published geometry passes;
53. final published frame rate passes;
54. final published duration passes;
55. final published stream/chapter evidence passes;
56. published state requires published validator PASS;
57. publication requires explicit Product Owner action;
58. successful publication goes directly into `DVD Upscaled`;
59. no per-movie destination folder is created;
60. final generated filename is shown before publication;
61. completion screen shows actual publication state;
62. completion screen shows actual validation state;
63. completion screen shows generated filename;
64. completion screen shows friendly destination identity;
65. no automatic move to Henderson Library occurs;
66. no Jellyfin action occurs;
67. no original-rip deletion occurs;
68. no automatic local-work cleanup occurs;
69. telemetry remains observational;
70. no new RVE profile is introduced;
71. no RVE installation component is changed;
72. no arbitrary model/backend control is introduced;
73. automated naming tests pass;
74. automated finalization-boundary tests pass;
75. automated remux tests pass;
76. automated local-validator tests pass;
77. automated destination-safety tests pass;
78. automated collision tests pass;
79. automated publication-staging tests pass;
80. automated restart/durable-state tests pass;
81. full automated suite passes;
82. `git diff --check` passes;
83. local live finalization succeeds without NAS publication first;
84. Product Owner reviews generated final filename;
85. Product Owner reviews narrow NAS write boundary;
86. Product Owner explicitly authorizes first live publication;
87. real publication completes successfully;
88. published MKV appears directly in `DVD Upscaled`;
89. no nested movie folder is created;
90. existing unrelated NAS media remains unchanged;
91. Product Owner confirms published filename is correct;
92. Product Owner confirms published file plays;
93. Product Owner confirms expected audio;
94. Product Owner confirms expected subtitles where applicable;
95. Product Owner confirms chapters where applicable;
96. Product Owner confirms original remains intact;
97. Product Owner confirms Henderson Library was not modified;
98. exactly one 0.1.6 closeout is produced;
99. Acceptance Matrix contains no required FAIL or NOT TESTED items;
100. coder stops for Product Owner review;
101. no 0.1.7 work begins.

---

# 68. Milestone Completion Boundary

Do not declare Milestone 0.1.6 complete merely because:

```text
final remux works locally
```

or because:

```text
NAS copy command succeeds
```

Completion requires the full accepted chain:

```text
authoritative RVE result
→ final remux
→ local validation PASS
→ Product Owner publication approval
→ narrow controlled NAS publication
→ published validation PASS
→ Product Owner filesystem/playback sanity validation
```

---

# 69. Expected Next Milestone

If 0.1.6 closes cleanly, the expected next milestone is:

```text
0.1.7 — Service Activation, Windows Launcher, and v1 Finishing
```

Expected scope:

```text
stable Linux service/autostart
Windows shortcut/icon launch
startup/restart behavior
small remaining operator polish
v1 end-to-end smoke test
operator documentation
```

Do not implement 0.1.7 during this milestone.

Stop after the 0.1.6 closeout and return control to the Product Owner.
