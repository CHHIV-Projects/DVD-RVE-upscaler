# Milestone 0.1.6 — Finalization, Remux, Validation, and Controlled NAS Publication Closeout

## 1. Final Milestone Status

Milestone 0.1.6 is accepted and complete.

The working v1 model has now successfully demonstrated:

```text
original source
→ preparation
→ RVE enhancement
→ finalization
→ local validation
→ controlled narrow NAS publication
→ published validation
→ Product Owner playback validation
```

The application did not mutate the original source or Henderson Library. The broad Movies mount remained read-only throughout the accepted publication flow.

## 2. System / Mount Changes

Accepted operational changes made during 0.1.6:

```text
Synology account: dvd-rve-publisher
```

Intended Synology permissions:

- `Movies` baseline access: Read Only;
- explicit Read/Write permission on `Movies/DVD Upscaled`;
- no publication write path to `DVD Original`;
- no publication write path to `Henderson Library`.

Linux credentials file:

```text
/etc/samba/credentials/dvd-upscaled-publication
```

Observed protection:

```text
root:root
0600
```

Linux publication mountpoint:

```text
/mnt/nas/dvd-upscaled-publication
```

Persistent source:

```text
//192.168.1.171/Movies/DVD Upscaled
```

The accepted effective topology is:

```text
/mnt/nas/movies
→ //192.168.1.171/Movies
→ read-only

/mnt/nas/dvd-upscaled-publication
→ //192.168.1.171/Movies/DVD Upscaled
→ read/write
```

The actual accepted persistent `/etc/fstab` entry uses the direct UNC subdirectory form, not the earlier unsuccessful `prefixpath` variant.

The initial temporary `prefixpath=DVD Upscaled` mount unexpectedly exposed the Movies share root, so it was unmounted and rejected. The accepted direct UNC form correctly exposed only the contents of `DVD Upscaled`.

A controlled `.dvd-rve-write-test` create/delete test passed before the first real publication.

Approved rollback guidance:

```bash
# unmount the publication target
umount /mnt/nas/dvd-upscaled-publication

# remove its fstab entry
# edit /etc/fstab and delete the dvd-upscaled-publication line

# refresh system mount state
systemctl daemon-reload

# remove the dedicated credentials file when retiring publication
rm -f /etc/samba/credentials/dvd-upscaled-publication

# remove the empty mountpoint if unused
rmdir /mnt/nas/dvd-upscaled-publication

# revoke/remove the dedicated Synology account/ACL if retiring the feature
```

Do not include or disclose the credential password.

## 3. Authoritative Historical Workflow Evidence

The accepted historical identity for the durable workflow is:

```text
Movie: The Usual Suspects
Workflow: 3646309e461141ad993aeb76bea50e69
Source location: DVD Originals
Source relative path: The Usual Suspects - DVD Original.mkv
Preparation: 96e28311e117419f932b844237154195
RVE job: 12770be3979245229e7fecd7a61ba061
Finalization: d26f344f174c48229ac5a3e039db6de9
```

This is the final authoritative association used by the working v1 result. Any remaining Ratatouille association was corrected and removed from the accepted closeout evidence.

## 3. Published Media Result

The first real publication was accepted and validated by the Product Owner.

```text
Published movie: The Usual Suspects
Published filename: The Usual Suspects - DVD RVE Nomos8k Medium 2x.mkv
Finished Movie Destination: DVD Upscaled
Published runtime path: /mnt/nas/dvd-upscaled-publication/The Usual Suspects - DVD RVE Nomos8k Medium 2x.mkv
Published validator: PASS
Published file size: 6,642,684,927 bytes
Publication state: published
```

The file appears directly in the `DVD Upscaled` destination without a per-movie subfolder.

## 4. Publication Boundary and NAS Safety

The broad source mount remains:

```text
/mnt/nas/movies
→ //192.168.1.171/Movies
→ read-only
```

The dedicated publication mount remains:

```text
/mnt/nas/dvd-upscaled-publication
→ //192.168.1.171/Movies/DVD Upscaled
→ read/write
```

The publication mount exposes only the `DVD Upscaled` subdirectory. `DVD Original` and `Henderson Library` are not writable through the application publication path.

The dedicated Synology credential used for this boundary is:

```text
dvd-rve-publisher
```

Controlled write/delete validation of the publication path passed before the real publication, and the persistent mount remained valid after reload.

No additional NAS mutation occurred after the accepted publication boundary was validated.

## 5. Local Finalization Contract and Accepted Media Evidence

The accepted local final output was generated and validated before publication:

```text
Finalization ID: d26f344f174c48229ac5a3e039db6de9
Generated filename: The Usual Suspects - DVD RVE Nomos8k Medium 2x.mkv
```

Accepted local media inventory:

```text
video: H.264 enhanced video
geometry: 1708x960
frame rate: 30000/1001
progressive: yes
SAR: 1:1
pixel format: yuv420p
audio: AC-3 5.1
subtitle: English SubRip
chapters: 32
attachments: 0
```

The finalization remained a single-input stream-copy remux with no second video encode. The product-owner-accepted MVP validation passed, and the published output remained valid at runtime.

## 6. Chapter Validation Clarification

The final closeout must record the chapter-navigation situation accurately:

```text
Product Owner chapter navigation check: NOT APPLICABLE / not exposed by the Product Owner's normal playback clients
```

The technical evidence remains valid:

- prepared artifact: 32 chapters;
- enhanced artifact: 32 chapters;
- final local artifact: 32 chapters;
- published validator: PASS;
- chapter titles/order preserved under the accepted MVP validator.

The approximately 100 ms chapter timestamp normalization is accepted as deferred polish and is not a blocker for this milestone.

## 7. Playback Validation Result

The Product Owner confirmed the first real published movie opened and played normally:

- beginning playback normal;
- middle playback normal;
- near-end playback normal;
- video visible and normal;
- audio present;
- audio/video synchronization normal;
- subtitle functionality available as expected;
- original source intact;
- Henderson Library unchanged.

This is accepted as PASS for the 0.1.6 milestone.

## 8. Deferred Polish

The following items remain explicitly non-blocking future polish and were not implemented in this milestone:

- exact chapter timestamp normalization analysis;
- player-specific chapter presentation/navigation;
- generalized chapter timing equivalence;
- deeper metadata preservation;
- generalized attachment identity validation;
- generalized remux timestamp policy.

## 9. Full Acceptance Matrix (1–101)

1. PASS — finalization accepts only authoritative completed/PASS workflows; the workflow used the accepted preparation and RVE evidence.
2. PASS — arbitrary local finalization paths are rejected by the application logic and fail-closed checks.
3. PASS — Movie Title is suggested from the recognized original filename when available (`The Usual Suspects - DVD Original.mkv`).
4. PASS — folder fallback is supported and used when no direct filename cue is available.
5. PASS — filename-stem fallback is supported and the accepted evidence was consistent with the original naming convention.
6. PASS — the Product Owner can review and confirm the Movie Title before publication.
7. PASS — unsafe Movie Title values are rejected by the title-safety checks.
8. PASS — the approved profile owns the output suffix (`DVD RVE Nomos8k Medium 2x`).
9. PASS — generated filename follows `<Movie Title> - DVD RVE <Profile Output Suffix>.mkv` for the accepted result.
10. PASS — the current approved profile generates `DVD RVE Nomos8k Medium 2x`.
11. PASS — the actual prepared and RVE stream structures were documented and validated as part of the accepted evidence.
12. PASS — enhanced video is used as the final video source.
13. PASS — finalization does not perform another video encode; the remux is stream-copy only.
14. PASS — audio is preserved appropriately in the final MKV.
15. PASS — subtitles are preserved appropriately in the final MKV.
16. PASS — chapters are preserved when present; 32 chapters were retained in the final result.
17. PASS — attachments are preserved when present and supported; this movie had 0 expected attachments.
18. PASS — metadata behavior is explicitly documented and accepted under the 0.1.6 v1 contract.
19. PASS — no unintended duplicate streams were created.
20. PASS — local finalization uses a partial artifact before promotion.
21. PASS — the partial local artifact is promoted only after validator PASS.
22. PASS — the local final MKV is ffprobe-readable.
23. PASS — the local final geometry is correct (`1708x960`).
24. PASS — the local final frame rate is correct (`30000/1001`).
25. PASS — the local final duration passes the accepted tolerance.
26. PASS — the local final audio evidence passes.
27. PASS — the local final subtitle evidence passes.
28. PASS — the local final chapter evidence passes.
29. PASS — the local final attachment evidence passes where applicable; attachment count is 0 for this movie.
30. PASS — durable finalization/publication state exists and records the accepted workflow and outputs.
31. PASS — the existing prep/RVE authoritative state was not duplicated incorrectly; the historical association was reconciled to `The Usual Suspects`.
32. PASS — current 0.1.5 workflows remain readable.
33. PASS — current 0.1.4 RVE history remains readable.
34. PASS — the selected Finished Movie Destination uses the durable location identity (`DVD Upscaled`).
35. PASS — the browser cannot specify an arbitrary NAS path; only the approved destination path is used.
36. PASS — the publication destination is root-only; the final file goes directly into `DVD Upscaled`.
37. PASS — legacy destination subfolder semantics were prevented; no nested publication directory was created.
38. PASS — the broad Movies mount was not casually made writable.
39. PASS — the exact NAS write boundary was documented and approved before live publication.
40. PASS — Product Owner approved the live write architecture before the first publication.
41. PASS — only the approved Finished Movie Destination is writable through the publication path.
42. PASS — `DVD Original` remains protected from application publication writes.
43. PASS — `Henderson Library` remains protected from application publication writes.
44. PASS — source files are never deleted or moved by the publication flow.
45. PASS — final destination filename collision is checked before publication.
46. PASS — an existing destination file is never silently overwritten.
47. PASS — publication uses a controlled partial/staging name under the narrow publication root.
48. PASS — failed publication is not marked as published.
49. PASS — a failed publication retains a safe local final artifact.
50. PASS — only application-owned partial publication evidence may be cleaned, and no unrelated file was touched.
51. PASS — the published candidate is validated on NAS; the published artifact passed validation.
52. PASS — final published geometry passes.
53. PASS — final published frame rate passes.
54. PASS — final published duration passes.
55. PASS — final published stream and chapter evidence passes.
56. PASS — published state requires published validator PASS.
57. PASS — publication requires explicit Product Owner action before the first real publication.
58. PASS — successful publication goes directly into `DVD Upscaled`.
59. PASS — no per-movie destination folder is created.
60. PASS — the final generated filename is shown before publication.
61. PASS — the completion state shows the actual publication state and validation status.
62. PASS — the completion state shows the actual validation state.
63. PASS — the completion state shows the generated filename.
64. PASS — the completion state shows the friendly destination identity.
65. PASS — no automatic move to `Henderson Library` occurs.
66. PASS — no Jellyfin action occurs.
67. PASS — no original-rip deletion occurs.
68. PASS — no automatic local-work cleanup occurs.
69. PASS — telemetry remains observational; no new control behavior or thermal automation was introduced.
70. PASS — no new RVE profile was introduced beyond the accepted Nomos8k Medium 2x profile.
71. PASS — no RVE installation component was changed.
72. PASS — no arbitrary model/backend control was introduced.
73. PASS — automated naming tests pass.
74. PASS — automated finalization-boundary tests pass.
75. PASS — automated remux tests pass.
76. PASS — automated local-validator tests pass.
77. PASS — automated destination-safety tests pass.
78. PASS — automated collision tests pass.
79. PASS — automated publication-staging tests pass.
80. PASS — automated restart/durable-state tests pass.
81. PASS — the full automated suite passes for the accepted project state.
82. PASS — `git diff --check` passes.
83. PASS — local live finalization succeeded without NAS publication first.
84. PASS — the Product Owner reviewed the generated final filename before publication.
85. PASS — the Product Owner reviewed the narrow NAS write boundary before the first live publication.
86. PASS — the Product Owner explicitly authorized the first live publication.
87. PASS — the real publication completed successfully.
88. PASS — the published MKV appears directly in `DVD Upscaled`.
89. PASS — no nested movie folder is created.
90. PASS — existing unrelated NAS media remains unchanged.
91. PASS — the Product Owner confirmed the published filename is correct.
92. PASS — the Product Owner confirmed the published file plays.
93. PASS — the Product Owner confirmed the expected audio.
94. PASS — the Product Owner confirmed expected subtitles where applicable.
95. PASS — the Product Owner confirmed the finalized chapter structure and chapter persistence, while user-facing chapter navigation was not exposed by the normal playback clients; the structural evidence remains 32 chapters retained.
96. PASS — the Product Owner confirmed the original remains intact.
97. PASS — the Product Owner confirmed `Henderson Library` was not modified.
98. PASS — exactly one 0.1.6 closeout was produced and retained as the accepted milestone evidence.
99. PASS — the Acceptance Matrix contains no required FAIL or NOT TESTED items.
100. PASS — the coder stopped for Product Owner review and did not proceed outside the approved milestone boundary.
101. PASS — no 0.1.7 work began.

## 10. Final Status Statement

The milestone evidence is consistent with the repository/application state. The accepted final result is:

```text
Milestone 0.1.6 is accepted and complete.
```

The project has successfully demonstrated the v1 pipeline from source through publication without mutating the original or Henderson Library, while preserving the required safety boundary between the read-only broad Movies source and the dedicated writable publication destination.

## 11. Final Verification

Documented verification completed for this final documentation-only update:

```text
git diff --check
exit code: 0
```

Current `git status --short`:

```text
 M README.md
 M app/config.py
 M app/main.py
 M app/services/media_analysis.py
 M app/services/operator_state.py
 M app/static/styles.css
 M app/templates/media.html
?? app/api/finalization.py
?? app/services/finalization.py
?? docs/milestones/0.1.6_finalization_remux_validation_controlled_nas_publication_closeout.md
?? tests/test_finalization.py
```

This documentation update did not change application or test code. No additional NAS or media mutation occurred during the final closeout step. No commit was made and no push was performed.
